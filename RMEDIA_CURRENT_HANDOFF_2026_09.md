# RMEDIA OS / MindBunker — Current Handoff
**Updated:** 2026-09-21 (Night Closure released; QA-clean) · Supersedes every earlier handoff and wave narrative. Read this plus the current source; older release paragraphs below are historical evidence unless explicitly marked current.

## Current release authority — Sep 21 Night Closure

Public and MindBunker/Operator now use **YouTube Red `#FF0000`** as the primary brand/interactive accent. This supersedes the older violet-primary descriptions below; Client Portal was not recoloured or deployed. The public Home and the separately routed `/book` Worker have finite entrance motion, reduced-motion immediate rendering, and zero infinite public animation; `/book` has a normal `href="/"` return link. `/start` changed accent tokens only (red for continuity), not questions, decision model, API or persistence.

`CRM → Inbound` (`/mindbunker/crm/inbound`) is an authenticated read model over the one canonical Lead plus immutable `guided_intake.submitted` events. Explicit registration of intake event types excludes manual Leads unless they later submit a real system intake. Multiple intakes remain distinct historical events, grouped per Lead. The durable badge counts **unseen system-intake events**, not Leads; opening only that Lead's unread intakes through the explicit POST action appends idempotent `system_intake.seen` events. GET routes write nothing. Source/referral, received time, schema-v1 payload and server-derived path remain on immutable events after mutable Lead edits. No second Lead store, notification table or migration.

| Authority | Current production truth |
|---|---|
| Operator source | Code commit `88252f6e8550de0238ff1e51245a9b2fc5852341`; this handoff's docs-only descendant is the final `release/video-workspace-hotfix = production/current` authority. |
| Operator Worker | `mindbunker` **`c87dba7e-ba59-4161-9501-24576daa3a5a`**; rollback `1362c77c-f19a-4603-adb6-b0d0214a3d6c`. |
| Public source | Local repo `rmedia-public-site`, commit **`1ce62599fdedf62afa45c2f8dd0879f963ae328a`**; no remote. This same commit contains the recovered canonical `/book` source at `book-worker/src/index.js`, `book-worker/public/index.html`, and its `wrangler.jsonc`. |
| Public Worker | `late-disk-3e57` **`e3e35906-ff1c-471b-b37c-217d94c2c7bb`**; rollback `d4ce7afe-65f7-4870-8508-371ed1ce5691`. |
| Booking Worker | Separate `rmedia-book` **`06bd4bd2-f275-489f-bf88-b75aee057b5d`**; rollback `55561e13-ee70-4d46-9cc5-61d7c98a6fcb`. Existing `/book*` route, D1 binding and booking/calendar logic were preserved. |
| Client/Sensor | Client Worker unchanged at `cf5be8f8-ae5a-426d-ac59-b59ee58620dc`; Sensor native app untouched. |
| D1 | `mindbunker` head **`0053_slow_shen.sql`**, no migration in this train. Pre-E2E and post-guarded-cleanup: **6 clients / 267 CRM events / 1 unread intake**, FK check empty. The unread event `270` on Lead `8` predated QA and remains untouched. |

Live E2E: one synthetic PDBM inquiry created Lead `9` and events `273` (`lead_created`) / `274` (`guided_intake.submitted`, schema 1, `REPEATABLE_PRODUCTION`, `referral:pdbm`). Inbound displayed the readable source/path and unread count `1 → 2`. Explicitly opening only QA Lead `9` appended ack event `275`, bringing unread `2 → 1`; repeated opening did not duplicate the ack. Seventeen downstream relationship families were zero. Exact guarded parent deletion removed Lead `9` plus its three cascading events (4 rows); QA selectors then returned zero, baseline `6/267/1` and FK integrity were restored. Public GET `/start` and `?ref=pdbm` produced zero writes.

Live visual QA passed on Home, `/book`, `/start`, PDBM, CRM/Inbound, Dashboard, War Room and Finance, including mobile 390×844 / 375×667 and public reduced-motion CSS. One separate finding: fresh loads of the **Sensor Activity web page** emit React hydration error `#418` in the browser console, while the page renders. This page's source was not changed in this train; treat the origin of the warning as unproven and do not label the whole operator console error-free. It is not a reason to rewrite or redeploy this frozen release without a separate diagnosis. See `RMEDIA_OS_NIGHT_CLOSURE_PRODUCTION_RELEASE_2026_09.md`.

## 1. What this is
A single-operator system for Emmanuel's freelance video-editing business (RMEDIA). One operator app (**MindBunker**, private) plus a public **Client Portal** and a public marketing site. The operator is an editor, not a marketing/BI person: the system must reduce reconstruction (Slack archaeology, folder hunting, status interpretation), never add data-clerk work. During production it should be quiet; **the next information should come from real work**, not new features.

## 2. Source and deploy authority (verify fresh; live reality wins)
| Surface | Truth |
|---|---|
| Operator repo | `github.com/z0rknight/mindbunker`. **Current Night Closure code/source is in the authority table above.** The earlier Guided Lead product commit was `8989ccce9fb32571b33e83c79cd0e9ced61077ec`. `main` remains an old unrelated branch: ignore. |
| Operator Worker | **Current version is in the Night Closure authority above.** Earlier final-convergence version was `1362c77c-f19a-4603-adb6-b0d0214a3d6c`; route `emmanueldarosa.com/mindbunker*`. Deploy only from reviewed canonical source: `npm run deploy`. |
| Client Worker | `white-wave-1af9`, version **`cf5be8f8-ae5a-426d-ac59-b59ee58620dc`** (final convergence build; rollback `4a4eb1c9-2ac2-45c8-b16e-c7ca705eb747`, the M3 client), `emmanueldarosa.com/client*`. Deploy: `npm run deploy:client` (only if client-facing code changes) |
| D1 | database `mindbunker` (`d6ada5db-…`), **migration head 0053** (`0053_slow_shen.sql`, applied 2026-09-20 19:06:51), FK check clean. **Current baseline is 6 clients / 267 CRM events / 1 unread intake**, per Night Closure above; the older 5/263 baseline was the Sep 20 checkpoint. Local `--local` remains a sandbox. |
| Public site | **Current source/version are in the Night Closure authority above.** Local `rmedia-public-site` has **no remote**. The former public version was `d4ce7afe-65f7-4870-8508-371ed1ce5691`. It owns `/start` and forwards submissions to the narrow same-origin Operator API through `MINDBUNKER`; `/book` is a distinct Worker. |
| Sensor | separate repo/worktree `mindbunker-sensor-release` @ `798ff57`, branch `release/sensor-consolidation-2026-09`; native macOS menu-bar app. Nothing native changed in the recent trains. |
Night Closure gate: 1478/1478 operator tests, 25/25 focused operator tests, 23/23 public tests, typecheck/build green, ESLint 0 errors + the same 3 warnings, sandbox A–F restored. Older gate counts below are historical.
Gates before any release: `git diff --check`, `npm test` (node:test, `*.test.mjs` under `src/modules|lib|utils`), `npx tsc --noEmit`, `npx eslint .` (**0 errors, 3 known pre-existing warnings**), `npm run build`. Night Closure last run: **1478/1478**. Stage files by explicit name (never `git add -A`); never bare `git stash`; preserve unrelated untracked files.

## 3. Product-role map (one primary question each)
Dashboard → general state today · **War Room** → what is happening now/next/blocked/active · **Productivity** → what is moving, Sensor Activity, Video Workspace · **Projects** → job context · **LET'S COOK** (Production Orders) → batches and what they contain · **Sessions** → what work actually happened (canonical Work Sessions) · **CRM** → lead/client relationship (owns client-level knowledge) · **Finance** → commercial/cash evidence and external-time attribution · **Client Portal** → only what the client may see · **Pricing Lab** → experimental, waiting for evidence. Known collisions (documented, not bugs): Dashboard/War Room both show "current work"; Productivity/Projects/Order pages all list videos; Sessions (canonical) vs Sensor Activity (observed).

## 4. Semantic invariants (do not break)
Sensor observed time ≠ Work Session ≠ external registered time ≠ billing evidence ≠ payment; allocation ≠ revenue; derived rate-equivalent ≠ transaction truth. READY_FOR_REVIEW ≠ APPROVED; DONE ≠ DELIVERED ≠ PAID. A Production Order has persisted `state` (OPEN/CLOSED/CANCELLED) and a derived `phase` (from active children, excluding the operational container and cancelled items); a closed order never claims completion. Production Order time ≠ per-video time (a batch-equivalent average is labelled and never written to a video). Observed app time ≠ intentional app time; Safari + ChatGPT page = APP Safari with an optional web surface, never native ChatGPT. **No automatic proportional spread** of external time across videos; "unallocated" is valid; unknown beats invented precision. Page views never create Leads. Production Memory, protected terms, export reminders, production context, attribution and evidence are **operator-only** (pinned by tests: nothing under `client-portal`, `app/client`, `app/g` may reference them). Health is protected from business-performance judgment; no personal scores. `returnTo` is always validated with `isSafeInternalPath`.

## 5. What is live (major features)
**Guided Lead Engine (production, QA-clean)** — public `/start` and `/start?ref=pdbm` use the approved card-driven flow; GET/page views perform zero writes. Submissions go same-origin to `/mindbunker/api/start`, where the server validates answers, resolves the closed referral allowlist, recomputes dimensions/starting path, lowercases email for canonical Lead reuse, and persists one immutable `guided_intake.submitted` event (`schemaVersion=1`) in migration 0053's nullable `crm_events.payload_json`. The CRM dossier projects the latest structured evidence as readable fields; it does not expose raw JSON or make pricing/acceptance decisions. Production E2E proved `REPEATABLE_PRODUCTION`, PDBM provenance, idempotent replay, and zero downstream Project/Video/Work Session/commercial/finance relationships. Synthetic Lead 7 + events 267/268 were then removed by exact guarded cleanup; baseline restored to 5 clients / 263 events with clean FKs. Release report: `RMEDIA_GUIDED_LEAD_ENGINE_PRODUCTION_RELEASE_2026_09.md`.

Production Order truth (shared status headline) · **PDBM referral**: `https://emmanueldarosa.com/mindbunker/quoteavideo?ref=pdbm` (closed allowlist in `modules/referrals`; source stored as `clients.source = 'referral:pdbm'`; CRM label "Referral · PDBM (Taryn / CEO Clubhouse)"; dedup/idempotency/honeypot unchanged; existing source never overwritten) · **Client Production Memory** (`client_production_memory`, managed in the CRM dossier, read-only "Formats for <client>" on Project/Order pages) · **Before-you-export** card in the Video Workspace (client protected terms + export reminders + a static two-line generic baseline; read-only, never gates a status) · **Production Context** block (batch notes, project notes with clickable links, project source media, review/delivery, format names) on Order and Video Workspace · origin-preserving navigation (signals, commitments, video↔order, Dashboard/CRM notes/Sessions carry `returnTo`) · optional revision cause (`revisions.caused_by`, default UNKNOWN) · **Application usage** on Sensor Activity (observed vs intentional, coverage disclosure, DELETED sessions excluded, overlaps not double-counted) · stale-timer re-sync on focus · **Batch evidence** block and **explicit external-time attribution** (Finance → Contracts → selected evidence; to a Production Order via its container, or a single video; least precise true level; only `MANUAL_MINUTES` written; same-client and over-allocation checks; one shared definition of "unallocated") · one-way "Add to Google Calendar" link (CRM and gateway booking).

**RMEDIA OS motion foundation (Train M1, live)** — additive presentation layer, no business change. Token contract (do not rename; the styleboard and the guided-intake prototype consume it): `--os-motion-fast/standard/panel/exit` (120/180/240/140ms × `--os-motion-scale`), `--os-ease`, `--os-ease-exit`, `--os-shift-sm/md`; scope with `data-intensity="public|client|operator"` (1.25 / 1 / 0.8; root body = operator, `client/layout.tsx` wrapper = client; derived tokens are re-declared on `[data-intensity]`). `prefers-reduced-motion` is honoured by real CSS (`html[data-motion="reduced"]` only forces it for tests). Primitives in `src/components/os` (`ActionButton`, `StatusTransition`, `UpdateFlash`, `useAction`, `usePendingGate`, `useUpdateFlash`) over pure logic in `src/lib/os` (`pending-gate`: no indicator under 150ms, >=300ms once shown). Proof surfaces only: `aria-current` on desktop + mobile nav, `CopyLinkButton` (StatusTransition + live region), War Room refresh control (ActionButton). No broad adoption; M2 (client approval/review/delivery) is the next candidate. Reference: `docs/design/rmedia-os-interaction-lab.*`.

**Client review / approval / delivery feedback (Train M2, live, client Worker only)** — presentation over the unchanged `transitionVideoStatusAsClient` (READY_FOR_REVIEW → DONE / CHANGES_REQUESTED; there is no APPROVED status: the client pill reads "Completed", or "Delivered" only with a delivery link). `src/modules/client-portal/review-flow.ts` is the small state machine (submit only from ready, success only from submitting = no optimistic Approved, double submit ignored, failure returns to ready with the server message). `ReviewActions` (container) + `ReviewActionsView` use the M1 primitives: pending cue only after ~150ms, one check-draw + thin success edge after `result.success`, persistent acknowledgement that survives the revalidation, focus moved to it, live-region announcement. `ClientStatusBadge` (label/colour transition), `DeliveryAvailability` (delivery is its own fact; brand tint, never the green approval treatment; pure decision in `delivery-state.ts`), `BatchProgress` (segmented composition in the dashboard's "In production now" section; review is never drawn as done). Truth fix: batch item labels now use `clientVideoStatusLabel` (DONE says "Delivered" only with a delivery link). No new-version state was added (no canonical fact exists for it). Payment motion and Finance stay deferred.

**Operator status / table / queue feedback (Train M3, live on the operator Worker; client Worker rebuilt for the read-model fix)** — presentation only, over unchanged actions and read models. Four patterns on real surfaces: **row/cell update** (`useChangedKeys` + `flashTarget`: one changed field marks its cell, several mark the row; applied to Work Session history rows after a correction), **server-truth resolve flow** (`lib/os/resolve-flow.ts` + `SensorSessionActions` on `ActionButton`/`useAction`: confirmation only after the server says yes; focus restored to the section heading even when the revalidated list already removed the row), **new arrival + change markers** (`ArrivalScope/ArrivalItem/useIsNew`, `useUpdateFlash`: War Room comandas, editor-station change + one polite announcement, client-table health (BLOCKED stays static), Needs Attention signals; the initial list is never "new", NEW is a word and expires in 8s), **queue counts** (`ValueChange` + `DepartureNotice` on Needs Attention and the Sensor inbox/long-session queues: crossfade + "N resolved" when items leave). Live truth: the Sensor connectivity card now shows a live ring only for CONNECTED + an open Work Session (`sensorIndicator`); idle/offline/no-device are static. `BatchProgress` moved to `components/os` and is shared with the operator Production Order deliverables. Read-model fix: client `recentDeliveries` lists each video once (newest completion event) before the top-5 cap; events untouched. Still deferred: payment/Finance motion, table-wide adoption, War Room ambient-loop retirement (M6), data-viz (M4). Next candidate **M4**.

**Evidence visualisation (Train M4, live on the operator Worker only)** — presentation of existing read models, no new data/page/dependency. One language: **`DataBar`** (neutral bar, exact value+units always printed, length relative to the longest row, violet only for a selected/subject row) and **`EvidenceRail`** (composition of a whole into known parts and an explicit UNKNOWN part; solid = source fact, hatched + the word "derived" = derived, dashed-empty + "unknown" = unknown; `role=img` text summary; over-allocation flagged, never clipped). Pure math in `lib/os/evidence-rail.ts` (`barPercent`, `buildRail`, `coverageParts`, `attributionParts`). Adopted on: Sensor **Application usage** (window switches move the same bars; first paint is static), the **intentional coverage rail** (active | idle | no telemetry, reconciles to session time; coverage % stays labelled), the Finance **registered-time attribution** panel and the batch-evidence weeks (attributed by you | historical derived | attributed elsewhere | unallocated), and the operator Production Order rail (operator wording, cancelled named but not counted, container never a deliverable). `ValueChange` on attribution stats and deliverable counts. Deferred by design: sparklines, timelines, the ACTIVE-view coverage rail (no strong current question), any BI page, pricing/profit views (Pricing Lab stays NOT READY). Next candidate **M5** (public + guided-intake motion) or **M6** (legacy loop cleanup).

**Final visual convergence (M5 + M6, live: public + operator + client)** — closure of the visual programme; no new design concepts. **/start**: same `--os-*` tokens, production motion tokens (public intensity), Inter, brand mark links home; a chosen card is acknowledged in place (no re-render) and then advances (260ms, 60ms under reduced motion); direction-aware step transition (forward/back, static on restore); progress = "Step n of N" + rail (no percentage); submit shows "Sending…" only after ~150ms (min 300ms), buttons use `aria-disabled`/`aria-busy` so focus stays, error is local and recoverable; keyboard focus ring on the hidden-radio cards; the sticky action bar exists only on the contact/review screens. Decision model, answer envelope and API untouched (its tests still pass). **Public site**: the idle-bob loops, card lift, glows and `transition: all` are gone (zero infinite animations live), one `:focus-visible` ring, and the inquiry CTAs (`Get a quote`, `Start a Project Inquiry`, `Start Your Inquiry`) now go to `/start` (Book a Call still `/book`; the old quote route stays reachable, unlinked). **Operator/client build**: War Room ambient loops retired (lamp flicker, editor pulse, review pulse; marker rings static; hover lift and glows removed — only the shared live ring still animates), stat-tile lift removed, three dialogs enter with the shared `os-arrive` animation, client scope neutralises pixel brackets/badges, `@theme --color-zinc-500: #8c8c96` fixes muted-text AA (664 uses), the token client page now reads "RMEDIA Client Portal". **Deferred (documented visual debt, not defects):** (public brand debt CLOSED in the next paragraph), emoji as navigation icons, cyan accent dominance, the 32px grid body background, dialog exit motion.

**Public brand convergence (closes the visual programme, 2026-09-20)** — the public marketing site now reads as the same product family as `/start`, Client Portal and MindBunker. Public repo commit **`6496f6a`** (local, no remote), Worker `late-disk-3e57` version **`d4ce7afe-65f7-4870-8508-371ed1ce5691`** (rollback `1eb74170-9849-44c3-9284-ff69a8ff36b1`). One stylesheet (`assets/css/style.css`) on the `/start` `--os-*` tokens: Inter (same Google Fonts link as `/start`), violet primary, neutral surfaces, system mono only for ≥12px labels, production motion tokens with the reduced-motion collapse, one `#b9aefb` focus ring. Retired from the public primary path: Lobby / Player HUD / XP bar / class cards / avatar slots / debuff rows / terminal cosplay, Press Start 2P (0 structural uses), red brand primary, sprite and pixel art (files deleted), the nine legacy CSS files. Navigation is literal (Work · Services · How it works · About); primary CTA "Start a project" → `/start` on every page, secondary "Book a 30-minute fit call" → `/book`; services became "Ways to work together" (no invented packages/prices, no internal enum names); the Work section keeps only the existing anonymized Content Waterfall case with a truthful frame placeholder; the founder essay is verbatim. Only entrance motion is the existing About `.reveal` (JS-gated, off under reduced motion). No dependency, business-logic, D1 or migration change; Operator/Client/Sensor not deployed. Public tests: `node --test test/` 18/18. **Visual programme: CLOSED.**

## 6. Current production facts worth preserving
**Taryn (client id 2):** Production Memory = Content Waterfall (OPERATOR_CONVENTION), Lecture Format (CLIENT_APPROVED; black treatment, white text box with black bold text, rounded corners; Taryn asked to keep it "in the vault as the lecture format"), Client Success Format (OBSERVED; teal, rounded corners). Template locations, approved references and most use-cases are NULL (human input). Protected terms: CEO Clubhouse, Perfect Day Business Mentorship, PDBM. Export reminders: correct person in B-roll; natural skin tone; horizontals fill the frame, no black bars. Deliberately omitted terms: Jannalee, 100 Lead Game, Buy Line (spelling unverified), CEO Strong (Bonnie's program), "Taryn".
**Commercial:** Taryn external registered time 69.5 h (Upwork, Jul 27–Sep 13, $25/h); explicit attribution 0 h; historical derived allocation 1.5 h; **unallocated 68.0 h (97.8%)**; billing evidence ≈$1,812; income transactions $1,372.50 (client level); Production Orders: 1; revisions recorded: 0. `billing_evidence` rows from Upwork are semantically EXTERNAL REGISTERED TIME (table name is historical). Dave "Landing Page" (video 25) `delivery_url` was canonicalized from Quick Note #75 (the only production write of that train).
**Sensor:** 1 device, 11k+ observations since Aug 24, **0 window titles** (browser surface unsupported until the operator enables titles); intentional sessions ≈240 h, ≈87% telemetry coverage; 4 sessions > 12 h await operator review; 6 legacy Aug 24–25 approved sessions lack a back-link but each has a matching Work Session. **Guided Lead/PDBM:** production E2E closed and synthetic evidence cleaned; **0 real Guided Leads known at closure**. **Quick Notes:** 37, none actionable (1 unknown: 4K-detail observation).

## 7. Waiting for evidence (do not build yet)
Video↔memory association · project-level external attribution · work-phase data · automated pre-export QA · PDBM conversion learning · Pricing Lab (**NOT READY**) · outbound marketing · Markdown notes · Sensor-native container labeling. Triggers are in `RMEDIA_BACKLOG_ARCHAEOLOGY_AND_CLOSURE_RELEASE_TRAIN_2026_09.md` §15.

## 8. Needs the operator (human input)
Template locations / approved reference videos / use-cases for the Taryn formats · spellings for the omitted terms · decide on enabling Sensor window titles (privacy) · whether the 4K-detail note still matters · Sensor stable signing identity and live GUI/sleep QA (the rebuilt ad-hoc signature resets Keychain/Accessibility grants) · comma-separated tags (domain decision).

## 9. Migration candidates — NOT approved
Nullable `billing_allocations.project_id` (at most one of video/project) · first-class cut-sheet/audio field · video→Production Memory FK · work-phase tag. None is justified today.

## 10. Explicitly do not build now
Pricing Lab / Offer Generator, BI dashboards, productivity/focus/sleep scores, OCR/transcription/computer-vision QA, Premiere integration, Google OAuth/two-way calendar, client-visible recipes, version history, DAM, a Quick Notes manager, more generic QA checks or terms without repeated real errors, referral platform/multi-referrer schema (until multiple referrers exist).

## 11. Working conventions
Authenticated production UI has usually NOT been observable by the assistant (no operator session; it never types the password); verification = local sandbox QA + read-only D1 (`wrangler d1 execute mindbunker --remote --command "SELECT …"`) + reachability. Local sandbox login: `/mindbunker/qa-login?token=…` (dev only; token in `.env.local`); restore fixtures after QA. D1 rejects large `UNION` chains and `PRAGMA foreign_key_list`. Prefer one release train per topic: one archaeology pass, targeted tests, one full gate, one deploy, one report.

## 12. Next
**REAL OPERATOR USE.** The visual programme (foundation, M1–M6) is CLOSED; start no further aesthetic wave without a concrete production defect. Observation targets (max 3): (1) the next real Guided/PDBM lead → confirm source, CRM evidence readability and the human handoff; (2) the next real Taryn batch → record revision causes where revisions happen and, when known, attribute that week's Upwork time to the batch; (3) review the 4 long Sensor sessions and decide about window titles.
