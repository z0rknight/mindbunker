// Moved to src/modules/client-portal/migration-0015.integration.test.mjs
// (matching this repo's convention -- see geladeira.integration.test.mjs --
// of keeping migration tests under the module they belong to, since
// package.json's `test` script only globs src/modules/**/*.test.mjs).
//
// This file could not be deleted from this session (the connected macOS
// folder's FUSE mount rejects unlink from this sandbox -- a known
// limitation, see the Sprint 1.2.2 round report). It is intentionally
// empty and not picked up by the test glob; please delete it by hand.
